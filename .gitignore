node_modules/
dist/
.env
*.log
logs/
.agency.lock

# Local memory database (structured state + vectors). Keep markdown in git, not the DB.
memory/agency.db
memory/agency.db-*

# Runtime data volume — DB_PATH points here by default (src/store.ts). Holds the live SQLite DB,
# the auto-generated MASTER_KEY file (data/.masterkey — a SECRET), and GitNexus caches. Never commit.
/data/
!web/data/

# Build-time generated (regenerated by scripts/version.mjs)
web/version.json
.DS_Store
