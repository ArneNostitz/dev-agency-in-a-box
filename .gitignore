node_modules/
dist/
.env
*.log
logs/
.agency.lock

# Local memory database (structured state + vectors). Keep markdown in git, not the DB.
memory/agency.db
memory/agency.db-*

# Cloned working copies of target repos
.work/

# Generated at Docker build (build stamp)
web/version.json
