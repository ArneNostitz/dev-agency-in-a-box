# The agency's team. Mention a handle in an issue to "pin" that teammate.
# Format:  @handle: role   (role is informational for now; Phase 3 routes by role)
# Keep handles short and easy to type. Lines starting with '#' are comments.
#
# Mention ANY of these in an issue's title or body to put the agency to work, e.g.:
#   "@dev add a /health endpoint"
# Today every handle maps to the single Developer agent; more roles arrive in Phase 3.

@dev: developer
@agency: developer
